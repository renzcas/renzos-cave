// cave/physics/infophyz-bridge.js
// Temporary stub bridge until full InfoPhyzx integration

export function computePhysics(state) {
  return {
    field: [0, 0, 0],
    curve: 0,
    density: 0
  };
}
